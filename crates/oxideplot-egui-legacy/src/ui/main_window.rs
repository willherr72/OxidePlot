// Main-window layout is now driven directly by `app::OxidePlotApp::update()`.
// This module is kept as a placeholder so that `ui::mod.rs` continues to
// compile without changes.  It can be extended later if the layout logic
// grows large enough to justify its own module.
